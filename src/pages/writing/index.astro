---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostCard from '../../components/PostCard.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('posts'))
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<BaseLayout title="Writing — Rohit Shrestha">
  <div class="listing-page">
    <p class="eyebrow">Writing</p>
    <h1 class="listing-title">Writing</h1>
    <p class="listing-intro">Practical notes on backend engineering, API design, resiliency, and health data platforms.</p>
  </div>
  <div class="writing-grid">
    {posts.length === 0 ? (
      <p>Nothing published yet. Check back soon.</p>
    ) : (
      posts.map((post) => <PostCard post={post} />)
    )}
  </div>
</BaseLayout>

<style>
  .listing-page {
    max-width: 720px;
    margin-bottom: 2rem;
  }

  .listing-intro {
    margin-top: .75rem;
    color: var(--muted);
    font-size: 1.08rem;
  }
</style>
